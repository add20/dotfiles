# git .completions
if [ ! -e ~/.bash/.git-completion.bash ]; then
  url="https://raw.github.com/git/git/master/contrib/completion/git-completion.bash"
  echo "getting the $url"
  curl "$url" > ~/.bash/.git-completion.bash
fi
unset url
source ~/.bash/.git-completion.bash
GIT_PS1_SHOWDIRTYSTATE=true

# npm completion
# . <(npm completion)
